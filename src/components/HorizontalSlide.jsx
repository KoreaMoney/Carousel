import React from "react";
import { Carousel } from "@mantine/carousel";
import "../styles/horizontalSlide.css";

const HorizontalSlide = () => {
  return (
    <div>
      가로 슬라이드<Carousel></Carousel>
    </div>
  );
};

export default HorizontalSlide;
